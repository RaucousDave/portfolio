import { AnimatePresence } from "framer-motion";

export default function AnimatedRoutes() {
  return (
    <AnimatePresence>
      {/* Route container reserved for multi-page extension */}
    </AnimatePresence>
  );
}
